-- ==========================================
-- SCRIPT [⚓] Nghia Senpai hub (PREMIUM EDITION)
-- ==========================================

local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TeleportService = game:GetService("TeleportService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local TweenService = game:GetService("TweenService")

local LocalPlayer = Players.LocalPlayer

local SPEED_MULTIPLIER = 3.0

local parentContainer = nil
local successCore = pcall(function() return CoreGui end)

if successCore and CoreGui then
    parentContainer = CoreGui
else
    parentContainer = LocalPlayer:WaitForChild("PlayerGui")
end

if parentContainer:FindFirstChild("NghiaSenpaiHub") then
    parentContainer.NghiaSenpaiHub:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "NghiaSenpaiHub"
ScreenGui.ResetOnSpawn = false
ScreenGui.DisplayOrder = 2147483647
ScreenGui.IgnoreGuiInset = true
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = parentContainer

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 340, 0, 650)
MainFrame.Position = UDim2.new(0.5, -170, 0.5, -325)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Visible = true
MainFrame.ZIndex = 10

Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 15)
local MainStroke = Instance.new("UIStroke", MainFrame)
MainStroke.Color = Color3.fromRGB(0, 255, 150)
MainStroke.Thickness = 2.5

local TitleBar = Instance.new("Frame", MainFrame)
TitleBar.Size = UDim2.new(1, 0, 0, 45)
TitleBar.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
Instance.new("UICorner", TitleBar).CornerRadius = UDim.new(0, 15)

local TitleText = Instance.new("TextLabel", TitleBar)
TitleText.Size = UDim2.new(1, -40, 0, 0)
TitleText.Position = UDim2.new(0, 15, 0, 0)
TitleText.BackgroundTransparency = 1
TitleText.Text = "⚓ NGHIA SENPAI HUB ⚓"
TitleText.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleText.Font = Enum.Font.GothamBlack
TitleText.TextSize = 14
TitleText.TextXAlignment = Enum.TextXAlignment.Left

local CloseBtn = Instance.new("TextButton", TitleBar)
CloseBtn.Size = UDim2.new(0, 28, 0, 28)
CloseBtn.Position = UDim2.new(1, -32, 0, 8)
CloseBtn.BackgroundColor3 = Color3.fromRGB(220, 50, 50)
CloseBtn.Text = "✕"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextSize = 14
Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(0, 8)

CloseBtn.MouseButton1Click:Connect(function() 
    MainFrame.Visible = false 
end)

UserInputService.InputBegan:Connect(function(input, gpe)
    if not gpe and (input.KeyCode == Enum.KeyCode.LeftControl or input.KeyCode == Enum.KeyCode.RightControl) then
        MainFrame.Visible = not MainFrame.Visible
    end
end)

local Container = Instance.new("ScrollingFrame", MainFrame)
Container.Size = UDim2.new(1, -16, 1, -55)
Container.Position = UDim2.new(0, 8, 0, 50)
Container.BackgroundTransparency = 1
Container.ScrollBarThickness = 4
Container.ScrollBarImageColor3 = Color3.fromRGB(0, 200, 120)
Container.AutomaticCanvasSize = Enum.AutomaticSize.Y
Container.ScrollingDirection = Enum.ScrollingDirection.Y
Container.CanvasSize = UDim2.new(0, 0, 0, 0)

local UIListLayout = Instance.new("UIListLayout", Container)
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
UIListLayout.Padding = UDim.new(0, 8)

local function AddSection(title)
    local section = Instance.new("Frame", Container)
    section.Size = UDim2.new(1, 0, 0, 30)
    section.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
    section.BackgroundTransparency = 0.7
    Instance.new("UICorner", section).CornerRadius = UDim.new(0, 6)
    
    local sectionText = Instance.new("TextLabel", section)
    sectionText.Size = UDim2.new(1, -20, 1, 0)
    sectionText.Position = UDim2.new(0, 10, 0, 0)
    sectionText.BackgroundTransparency = 1
    sectionText.Text = title
    sectionText.TextColor3 = Color3.fromRGB(255, 255, 255)
    sectionText.Font = Enum.Font.GothamBold
    sectionText.TextSize = 12
    sectionText.TextXAlignment = Enum.TextXAlignment.Left
    return section
end

local function AddButton(name, color, callback)
    local btn = Instance.new("TextButton", Container)
    btn.Size = UDim2.new(1, 0, 0, 38)
    btn.BackgroundColor3 = color or Color3.fromRGB(30, 30, 40)
    btn.Text = name
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 11
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
    btn.MouseButton1Click:Connect(callback)
    return btn
end

local function AddToggle(name, callback)
    local btn = Instance.new("TextButton", Container)
    btn.Size = UDim2.new(1, 0, 0, 38)
    btn.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
    btn.Text = name .. " : [OFF]"
    btn.TextColor3 = Color3.fromRGB(180, 180, 180)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 11
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
    
    local enabled = false
    local function updateState(newState)
        enabled = newState
        btn.Text = name .. " : " .. (enabled and "[ON]" or "[OFF]")
        btn.BackgroundColor3 = enabled and Color3.fromRGB(0, 160, 80) or Color3.fromRGB(25, 25, 35)
        btn.TextColor3 = enabled and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(180, 180, 180)
        callback(enabled)
    end
    btn.MouseButton1Click:Connect(function() updateState(not enabled) end)
    return { SetState = updateState, GetState = function() return enabled end }
end

local function AddInput(labelTitle, defaultVal, callback)
    local frame = Instance.new("Frame", Container)
    frame.Size = UDim2.new(1, 0, 0, 38)
    frame.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 8)

    local label = Instance.new("TextLabel", frame)
    label.Size = UDim2.new(0.6, 0, 1, 0)
    label.Position = UDim2.new(0, 8, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = labelTitle
    label.TextColor3 = Color3.fromRGB(180, 180, 180)
    label.Font = Enum.Font.GothamBold
    label.TextSize = 11
    label.TextXAlignment = Enum.TextXAlignment.Left

    local box = Instance.new("TextBox", frame)
    box.Size = UDim2.new(0.35, -6, 0.7, 0)
    box.Position = UDim2.new(0.65, 0, 0.15, 0)
    box.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    box.Text = tostring(defaultVal)
    box.TextColor3 = Color3.fromRGB(255, 255, 255)
    box.Font = Enum.Font.GothamBold
    box.TextSize = 11
    Instance.new("UICorner", box).CornerRadius = UDim.new(0, 6)

    box.FocusLost:Connect(function()
        local val = box.Text
        if tonumber(val) then
            callback(tonumber(val))
        else
            callback(val)
        end
    end)
end

local function showNotification(message, duration)
    duration = duration or 3
    local notification = Instance.new("Frame")
    notification.Size = UDim2.new(0, 280, 0, 50)
    notification.Position = UDim2.new(0.5, -140, 0.1, 0)
    notification.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
    notification.ZIndex = 999
    notification.Parent = ScreenGui
    Instance.new("UICorner", notification).CornerRadius = UDim.new(0, 10)
    
    local notifText = Instance.new("TextLabel", notification)
    notifText.Size = UDim2.new(1, -20, 1, 0)
    notifText.Position = UDim2.new(0, 10, 0, 0)
    notifText.BackgroundTransparency = 1
    notifText.Text = message
    notifText.TextColor3 = Color3.fromRGB(255, 255, 255)
    notifText.Font = Enum.Font.GothamBold
    notifText.TextSize = 12
    notifText.TextWrapped = true
    
    task.delay(duration, function()
        notification:Destroy()
    end)
end

-- Biến toàn cục
local isFlyingToIsland = false
local currentFlyTween = nil

-- Hàm dừng bay
local function stopFlying()
    isFlyingToIsland = false
    if currentFlyTween then
        pcall(function() currentFlyTween:Cancel() end)
        currentFlyTween = nil
    end
    local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if root then
        root.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
    end
    local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
    if hum then
        hum.PlatformStand = false
    end
end

-- ==========================================
-- SECTION 1: 🏠 HOME
-- ==========================================
AddSection("🏠 HOME")

AddButton("🏠 Teleport Về Home", Color3.fromRGB(0, 200, 255), function()
    pcall(function()
        local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if not root then return end
        local homePosition = nil
        for _, obj in ipairs(workspace:GetChildren()) do
            if obj:IsA("SpawnLocation") then
                homePosition = obj.Position
                break
            end
        end
        if not homePosition then
            for _, obj in ipairs(workspace:GetDescendants()) do
                if obj:IsA("SpawnLocation") then
                    homePosition = obj.Position
                    break
                end
            end
        end
        if homePosition then
            root.CFrame = CFrame.new(homePosition + Vector3.new(0, 5, 0))
            showNotification("✅ Đã teleport về Home!")
        else
            showNotification("❌ Không tìm thấy SpawnLocation!")
        end
    end)
end)

-- ==========================================
-- SECTION 2: 🎮 CHỨC NĂNG CƠ BẢN
-- ==========================================
AddSection("🎮 CHỨC NĂNG CƠ BẢN")

-- Chạy nhanh
local walkSpeedVal = 50 * SPEED_MULTIPLIER
local speedEnabled = false

local function applyWalkSpeed()
    local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
    if hum then
        hum.WalkSpeed = speedEnabled and walkSpeedVal or 16
    end
end

AddToggle("🏃 Chạy Nhanh", function(state)
    speedEnabled = state
    applyWalkSpeed()
end)

AddInput("⚙️ Tốc Độ Chạy:", walkSpeedVal, function(v)
    walkSpeedVal = tonumber(v) or 150
    if speedEnabled then applyWalkSpeed() end
end)

LocalPlayer.CharacterAdded:Connect(function()
    task.wait(0.5)
    if speedEnabled then applyWalkSpeed() end
end)

-- Nhảy cao
local jumpPowerVal = 120 * SPEED_MULTIPLIER
local jumpEnabled = false

AddToggle("🦘 Nhảy Cao", function(state)
    jumpEnabled = state
end)

UserInputService.JumpRequest:Connect(function()
    if jumpEnabled then
        local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if root then
            root.AssemblyLinearVelocity = Vector3.new(root.AssemblyLinearVelocity.X, jumpPowerVal, root.AssemblyLinearVelocity.Z)
        end
    end
end)

-- Tàng hình
local invisibleEnabled = false

AddToggle("👻 Tàng Hình", function(state)
    invisibleEnabled = state
    pcall(function()
        local char = LocalPlayer.Character
        if char then
            for _, part in ipairs(char:GetDescendants()) do
                if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                    part.Transparency = state and 1 or 0
                end
            end
        end
    end)
end)

-- Noclip
local noclip = false
local noclipConnection = nil

AddToggle("🚪 Xuyên Tường (Noclip)", function(state)
    noclip = state
    if noclipConnection then noclipConnection:Disconnect() noclipConnection = nil end
    if noclip then
        noclipConnection = RunService.Stepped:Connect(function()
            if noclip and LocalPlayer.Character then
                for _, part in ipairs(LocalPlayer.Character:GetDescendants()) do
                    if part:IsA("BasePart") then part.CanCollide = false end
                end
            end
        end)
    end
end)

-- Bay tự do
local flySpeed = 150
local isFlying = false
local flyConn, flyBV, flyBG

AddToggle("🕊️ Chức Năng Bay", function(state)
    isFlying = state
    local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
    if not root or not hum then return end

    if state then
        hum.PlatformStand = true
        flyBV = Instance.new("BodyVelocity", root)
        flyBV.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
        flyBG = Instance.new("BodyGyro", root)
        flyBG.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
        flyBG.D = 1000
        flyBG.P = 50000
        flyBG.CFrame = root.CFrame
        flyConn = RunService.RenderStepped:Connect(function()
            if not isFlying then return end
            local cam = workspace.CurrentCamera
            local dir = Vector3.new()
            if UserInputService:IsKeyDown(Enum.KeyCode.W) then dir += cam.CFrame.LookVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.S) then dir -= cam.CFrame.LookVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.D) then dir += cam.CFrame.RightVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.A) then dir -= cam.CFrame.RightVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.Space) then dir += Vector3.new(0,1,0) end
            if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then dir -= Vector3.new(0,1,0) end
            flyBG.CFrame = cam.CFrame
            flyBV.Velocity = dir.Magnitude > 0 and dir.Unit * flySpeed or Vector3.new(0,0,0)
        end)
    else
        if flyConn then flyConn:Disconnect() end
        if flyBV then flyBV:Destroy() end
        if flyBG then flyBG:Destroy() end
        hum.PlatformStand = false
    end
end)

AddInput("⚙️ Tốc Độ Bay Tự Do:", flySpeed, function(v)
    flySpeed = tonumber(v) or 450
end)

-- ==========================================
-- SECTION 3: 🏝️ BAY ĐẾN ĐẢO
-- ==========================================
AddSection("🏝️ BAY ĐẾN ĐẢO")

local islandFlySpeed = 300
AddInput("⚡ Tốc Độ Bay Đến Đảo:", islandFlySpeed, function(v)
    islandFlySpeed = tonumber(v) or 300
end)

local function flyToExactPosition(targetPos, islandName)
    local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
    if not root or not hum then return end
    
    stopFlying()
    isFlyingToIsland = true
    hum.PlatformStand = true
    
    local startPos = root.Position
    local flyHeight = math.max(startPos.Y, targetPos.Y) + 150
    local highPoint = Vector3.new(startPos.X, flyHeight, startPos.Z)
    local targetHigh = Vector3.new(targetPos.X, flyHeight, targetPos.Z)
    
    showNotification("🚁 Đang bay đến " .. islandName .. "...")
    
    local tween1 = TweenService:Create(root, TweenInfo.new(2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {CFrame = CFrame.new(highPoint)})
    currentFlyTween = tween1
    tween1:Play()
    
    tween1.Completed:Connect(function()
        if not isFlyingToIsland then return end
        local distance = (targetHigh - highPoint).Magnitude
        local duration = math.clamp(distance / islandFlySpeed, 2, 8)
        
        local tween2 = TweenService:Create(root, TweenInfo.new(duration, Enum.EasingStyle.Linear), {CFrame = CFrame.new(targetHigh)})
        currentFlyTween = tween2
        tween2:Play()
        
        tween2.Completed:Connect(function()
            if not isFlyingToIsland then return end
            local tween3 = TweenService:Create(root, TweenInfo.new(2.5, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {CFrame = CFrame.new(targetPos)})
            currentFlyTween = tween3
            tween3:Play()
            
            tween3.Completed:Connect(function()
                if not isFlyingToIsland then return end
                isFlyingToIsland = false
                hum.PlatformStand = false
                root.CFrame = CFrame.new(targetPos)
                root.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                showNotification("✅ Đã đáp đất tại " .. islandName .. "!")
            end)
        end)
    end)
end

local islandList = {
    {Name = " Dragon Island", Position = Vector3.new(-37.6, 828.3, -2732.2)},
    {Name = " Bee Island", Position = Vector3.new(101.6, 14.1, -1152.2)},
    {Name = " Cave Island", Position = Vector3.new(-3213.9, 9.9, -2839.7)},
    {Name = " Mountain Heights", Position = Vector3.new(-1611.9, 5.8, -5013.9)},
    {Name = " Plushie Island", Position = Vector3.new(2300.6, 15.5, -249.7)},
    {Name = " Cloud Island", Position = Vector3.new(-9667.8, 5328, -1339.2)},
    {Name = " Sunken Island", Position = Vector3.new(2071.2, 11, -2628.4)},
    {Name = " Lost Docks", Position = Vector3.new(1411.7, 16.3, -4400)},
    {Name = " Forgotten Depths", Position = Vector3.new(12.7, 6.4, -4945.5)},
    {Name = " Safari Island", Position = Vector3.new(-2243.8, 9.9, -2838.8)},
    {Name = " Volcano Island", Position = Vector3.new(-2283.2, 7.3, -1883.3)},
}

for _, island in ipairs(islandList) do
    AddButton("🚁 " .. island.Name, Color3.fromRGB(0, 150, 130), function()
        flyToExactPosition(island.Position, island.Name)
    end)
end

AddButton(" Dừng Bay", Color3.fromRGB(200, 50, 50), function()
    stopFlying()
    showNotification(" Đã dừng bay!")
end)

-- ==========================================
-- SECTION 4: 💾 LƯU VỊ TRÍ & BAY ĐẾN VỊ TRÍ LƯU
-- ==========================================
AddSection("💾 LƯU VỊ TRÍ & BAY")

local savedPosition = nil

AddButton(" Lưu Vị Trí Hiện Tại", Color3.fromRGB(0, 150, 200), function()
    local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if root then
        savedPosition = root.CFrame
        showNotification("✅ Đã lưu vị trí!")
    end
end)

AddButton(" Teleport Đến Vị Trí Đã Lưu", Color3.fromRGB(0, 200, 100), function()
    local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if root and savedPosition then
        root.CFrame = savedPosition
        showNotification("✅ Đã teleport!")
    else
        showNotification("❌ Chưa lưu vị trí!")
    end
end)

local flyToSavedSpeed = 300
AddInput(" Tốc Độ Bay Đến Vị Trí Lưu:", flyToSavedSpeed, function(v)
    flyToSavedSpeed = tonumber(v) or 500
end)

AddButton(" Bay Đến Vị Trí Đã Lưu", Color3.fromRGB(0, 200, 255), function()
    if not savedPosition then
        showNotification("❌ Hãy lưu vị trí trước!")
        return
    end
    local targetPos = savedPosition.Position
    flyToExactPosition(targetPos, "Vị Trí Đã Lưu")
end)

-- ==========================================
-- SECTION 5: 💰 MERCHANT
-- ==========================================
AddSection("💰 MERCHANT")

AddButton(" Teleport Đến Merchant", Color3.fromRGB(255, 200, 0), function()
    local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not root then return end
    for _, obj in ipairs(workspace:GetDescendants()) do
        pcall(function()
            local objName = obj.Name:lower()
            if objName:find("merchant") or objName:find("shop") or objName:find("trader") or objName:find("vendor") then
                if obj:IsA("Model") then
                    local primaryPart = obj.PrimaryPart or obj:FindFirstChildOfClass("BasePart")
                    if primaryPart then
                        root.CFrame = CFrame.new(primaryPart.Position + Vector3.new(0, 5, 0))
                        showNotification("✅ Đã teleport đến Merchant!")
                        return
                    end
                elseif obj:IsA("BasePart") then
                    root.CFrame = CFrame.new(obj.Position + Vector3.new(0, 5, 0))
                    showNotification("✅ Đã teleport đến Merchant!")
                    return
                end
            end
        end)
    end
    showNotification("❌ Không tìm thấy Merchant!")
end)

-- ==========================================
-- SECTION 6: 🍎 TELEPORT QUẢ
-- ==========================================
AddSection(" TELEPORT QUẢ")

local farmItems = {
    {Name = "Pepper", SearchTerms = {"pepper", "chili", "spicy"}, Color = Color3.fromRGB(255, 50, 50)},
    {Name = "Coconut", SearchTerms = {"coconut", "coco"}, Color = Color3.fromRGB(139, 69, 19)},
    {Name = "Yarn Ball", SearchTerms = {"yarn", "ball", "yarnball"}, Color = Color3.fromRGB(255, 105, 180)},
}

for _, item in ipairs(farmItems) do
    AddButton("🍎 Teleport Đến " .. item.Name, item.Color, function()
        local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if not root then return end
        local items = {}
        for _, obj in ipairs(workspace:GetDescendants()) do
            pcall(function()
                local objName = obj.Name:lower()
                for _, term in ipairs(item.SearchTerms) do
                    if objName:find(term) then
                        if obj:IsA("BasePart") then
                            table.insert(items, obj)
                        elseif obj:IsA("Model") then
                            local primaryPart = obj.PrimaryPart or obj:FindFirstChildOfClass("BasePart")
                            if primaryPart then table.insert(items, primaryPart) end
                        end
                        break
                    end
                end
            end)
        end
        if #items > 0 then
            local nearest = items[1]
            local nearestDist = (nearest.Position - root.Position).Magnitude
            for _, itemPart in ipairs(items) do
                local dist = (itemPart.Position - root.Position).Magnitude
                if dist < nearestDist then
                    nearest = itemPart
                    nearestDist = dist
                end
            end
            root.CFrame = CFrame.new(nearest.Position + Vector3.new(0, 5, 0))
            showNotification("✅ Đã teleport đến " .. item.Name)
        else
            showNotification("❌ Không tìm thấy " .. item.Name)
        end
    end)
end

-- ==========================================
-- SECTION 7: 🖱️ AUTO CLICK
-- ==========================================
AddSection(" AUTO CLICK")

local autoClicker = false
local autoClickInterval = 0.1

local function startAutoClick()
    task.spawn(function()
        while autoClicker do
            pcall(function()
                local cam = workspace.CurrentCamera
                if cam then
                    local clickX = cam.ViewportSize.X * 0.85
                    local clickY = cam.ViewportSize.Y * 0.5
                    VirtualInputManager:SendMouseButtonEvent(clickX, clickY, 0, true, game, 0)
                    VirtualInputManager:SendMouseButtonEvent(clickX, clickY, 0, false, game, 0)
                end
            end)
            task.wait(autoClickInterval)
        end
    end)
end

local autoClickToggle = AddToggle("🖱️ Auto Click (Ấn Y)", function(state)
    autoClicker = state
    if state then startAutoClick() end
end)

UserInputService.InputBegan:Connect(function(input, gpe)
    if not gpe and input.KeyCode == Enum.KeyCode.Y then
        autoClickToggle.SetState(not autoClickToggle.GetState())
    end
end)

-- ==========================================
-- SECTION 8: 🔗 SERVER ID
-- ==========================================
AddSection("🔗 SERVER ID")

AddButton("📋 Sao Chép ID Server", Color3.fromRGB(0, 150, 255), function()
    local serverId = game.JobId
    if setclipboard then
        setclipboard(serverId)
        showNotification("✅ Đã sao chép!")
    else
        showNotification("📋 ID: " .. serverId, 5)
    end
end)

local ServerIdBox = Instance.new("TextBox", Container)
ServerIdBox.Size = UDim2.new(1, 0, 0, 38)
ServerIdBox.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
ServerIdBox.PlaceholderText = "Nhập ID Server..."
ServerIdBox.TextColor3 = Color3.fromRGB(255, 255, 255)
ServerIdBox.Font = Enum.Font.GothamBold
ServerIdBox.TextSize = 11
Instance.new("UICorner", ServerIdBox).CornerRadius = UDim.new(0, 8)

AddButton("🚀 Vào Server ID Đã Nhập", Color3.fromRGB(0, 200, 100), function()
    local serverId = ServerIdBox.Text
    if serverId and serverId ~= "" then
        TeleportService:TeleportToPlaceInstance(game.PlaceId, serverId, LocalPlayer)
    else
        showNotification("❌ Hãy nhập ID Server!")
    end
end)

-- ==========================================
-- SECTION 9: ⚙️ KHÁC
-- ==========================================
AddSection("⚙️ KHÁC")

AddButton(" Tăng FPS", Color3.fromRGB(160, 40, 200), function()
    setfpscap(9999)
    showNotification("⚡ Đã tăng FPS!")
end)

AddButton(" Rejoin Server", Color3.fromRGB(40, 120, 80), function()
    TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, LocalPlayer)
end)

AddButton(" Reset Nhân Vật", Color3.fromRGB(180, 90, 0), function()
    local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
    if hum then hum.Health = 0 end
end)

-- Toggle Button
local ToggleButton = Instance.new("TextButton", ScreenGui)
ToggleButton.Size = UDim2.new(0, 55, 0, 55)
ToggleButton.Position = UDim2.new(0, 15, 0.4, 0)
ToggleButton.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
ToggleButton.Text = "⚓"
ToggleButton.TextColor3 = Color3.fromRGB(0, 220, 130)
ToggleButton.Font = Enum.Font.GothamBlack
ToggleButton.TextSize = 20
ToggleButton.ZIndex = 100
Instance.new("UICorner", ToggleButton).CornerRadius = UDim.new(1, 0)
ToggleButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)

print("[Nghia Senpai Hub] Script đã tải thành công!")
